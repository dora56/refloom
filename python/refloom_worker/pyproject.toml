[project]
name = "refloom-worker"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "PyMuPDF>=1.25",
    "ebooklib>=0.18",
    "beautifulsoup4>=4.13",
]

[tool.pytest.ini_options]
testpaths = ["."]

[dependency-groups]
dev = ["pytest>=8.0"]
